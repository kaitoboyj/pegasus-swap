import React from 'react';
import DonateCharity from './DonateCharity';

// Render the integrated charity UI directly inside the app
const Donate: React.FC = () => {
  return <DonateCharity />;
};

export default Donate;